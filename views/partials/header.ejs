<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Source Code Hub | Legends</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;700&family=Space+Grotesk:wght@300;500;700&display=swap" rel="stylesheet">
    
    <style>
        body {
            background-color: #000000;
            color: #ffffff;
            font-family: 'Space Grotesk', sans-serif;
            overflow-x: hidden;
        }
        .mono { font-family: 'JetBrains Mono', monospace; }
        
        /* Cyber-Border & Glow */
        .cyber-card {
            border: 1px solid #27272a; /* Zinc 800 */
            transition: all 0.3s ease;
        }
        .cyber-card:hover {
            border-color: #FF0000;
            box-shadow: 0 0 20px rgba(255, 0, 0, 0.2);
            transform: translateY(-2px);
        }
        .red-glow {
            text-shadow: 0 0 10px rgba(255, 0, 0, 0.8);
        }
        .btn-red {
            background: #FF0000;
            color: white;
            transition: all 0.3s ease;
        }
        .btn-red:hover {
            box-shadow: 0 0 25px rgba(255, 0, 0, 0.6);
            transform: scale(1.05);
        }
        /* Custom Scrollbar */
        ::-webkit-scrollbar { width: 5px; }
        ::-webkit-scrollbar-track { background: #000; }
        ::-webkit-scrollbar-thumb { background: #FF0000; }
    </style>
</head>
<body class="pb-24">
    <header class="fixed top-0 w-full z-50 bg-black/80 backdrop-blur-xl border-b border-zinc-900">
        <div class="max-w-6xl mx-auto px-6 h-20 flex items-center justify-between">
            <a href="/" class="text-2xl font-bold tracking-tighter flex items-center gap-2">
                <span class="text-[#FF0000]">&lt;/&gt;</span> SOURCE CODE
            </a>
            
            <a href="/legends-info" class="flex items-center gap-2 px-5 py-2 rounded-full border border-zinc-800 hover:border-[#FF0000] transition-all group">
                <span class="text-sm font-medium">INFO</span>
                <span class="group-hover:animate-bounce">👑</span>
            </a>
        </div>
    </header>
